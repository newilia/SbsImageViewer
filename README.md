# VR Stereo Image Viewer

Просмотрщик стереоизображений для VR-шлемов с использованием OpenXR.

## Возможности

- ✅ Просмотр стереоизображений в формате Side-by-Side (SBS)
- ✅ Поддержка отдельных файлов для левого и правого глаза
- ✅ Совместимость со всеми VR-шлемами через OpenXR (Quest, Index, Vive, WMR и др.)
- ✅ Управление масштабом и расстоянием до изображения
- ✅ Переключение между изображениями
- ✅ Поддержка популярных форматов (JPG, PNG, BMP, TIFF)

## Требования

- Windows 10/11
- Python 3.8+
- VR-шлем с поддержкой OpenXR (Meta Quest, Valve Index, HTC Vive, Windows Mixed Reality и др.)
- Установленный OpenXR Runtime (SteamVR, Oculus, WMR)

## Установка

### Быстрая установка (Windows)

Просто запустите:
```
install.cmd
```

Скрипт автоматически:
- Создаст виртуальное окружение
- Установит все зависимости
- Проверит корректность установки

### Ручная установка

#### 1. Клонируйте репозиторий или скачайте файлы

```bash
git clone <repository-url>
cd SbsImageViewer
```

#### 2. Создайте виртуальное окружение (рекомендуется)

```bash
python -m venv venv
venv\Scripts\activate
```

#### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройка OpenXR Runtime

Убедитесь, что у вас установлен и настроен OpenXR runtime:

- **SteamVR**: Установите SteamVR из Steam
- **Meta Quest (Link/Air Link)**: Установите Oculus PC App
- **Windows Mixed Reality**: Включено в Windows

## Использование

### Графический интерфейс (рекомендуется)

Дважды кликните на:
```
VR Stereo Viewer.cmd
```

Откроется красивое окно, куда можно:
- 📁 Перетащить файлы или папки
- Нажать кнопку выбора файлов/папки
- Запустить просмотр в VR одной кнопкой

![Launcher](https://via.placeholder.com/400x300/1a1a2e/eaeaea?text=VR+Stereo+Viewer)

### Консольный запуск

Дважды кликните на:
```
run.cmd
```

### Простой запуск (без командной строки)

Просто запустите приложение без параметров — откроется меню выбора:

```bash
python sbs_viewer.py
```

Вы увидите меню:
```
=== VR Stereo Image Viewer ===

Выберите способ открытия изображений:
  1. Выбрать файлы
  2. Выбрать папку
  3. Выход

Ваш выбор (1/2/3):
```

### Запуск с файлами из командной строки

```bash
# Просмотр одного SBS изображения
python sbs_viewer.py stereo_image.jpg

# Просмотр всех изображений в папке
python sbs_viewer.py ./my_stereo_photos/

# Просмотр нескольких файлов
python sbs_viewer.py image1.jpg image2.png image3.jpg
```

### Открытие файлов во время работы

Во время просмотра в VR вы можете открыть новые изображения несколькими способами:

- **Drag & Drop** — перетащите файлы или папку на окно приложения
- Нажмите **O** — откроется диалог выбора файлов
- Нажмите **F** — откроется диалог выбора папки

> 💡 При запуске приложения появляется небольшое окно "VR Stereo Viewer" — на него можно перетаскивать файлы в любое время.

### Режим отдельных файлов

Если у вас отдельные файлы для левого и правого глаза:

```bash
python sbs_viewer.py --separate left_image.jpg right_image.jpg
```

Также поддерживается автоматическое сопоставление файлов с суффиксами `_left` и `_right`:

```bash
python sbs_viewer.py --separate photo_left.jpg photo_right.jpg
```

### Параметры командной строки

| Параметр | Описание |
|----------|----------|
| `images` | Путь к изображениям или директории |
| `--separate, -s` | Режим раздельных файлов (вместо SBS) |
| `--distance, -d` | Начальное расстояние до изображения в метрах (по умолчанию: 2.0) |
| `--scale` | Начальный масштаб изображения (по умолчанию: 1.5) |

### Примеры

```bash
# С настройкой расстояния и масштаба
python sbs_viewer.py --distance 3.0 --scale 2.0 image.jpg

# Просмотр всех PNG в папке
python sbs_viewer.py ./vr_photos/*.png
```

## Управление

| Действие | Описание |
|----------|----------|
| **Drag & Drop** | **Перетащите файлы/папку на окно приложения** |
| `O` | Открыть файлы (диалог выбора) |
| `F` | Открыть папку (диалог выбора) |
| `→` / `D` | Следующее изображение |
| `←` / `A` | Предыдущее изображение |
| `+` / `=` | Увеличить масштаб |
| `-` | Уменьшить масштаб |
| `W` | Приблизить изображение |
| `S` | Отдалить изображение |
| `ESC` / `Q` | Выход |

## Форматы изображений

### Side-by-Side (SBS)

Наиболее распространённый формат для стереофото и VR-контента. Левое и правое изображения расположены рядом в одном файле:

```
┌────────────────────────────────┐
│  Левый глаз  │  Правый глаз   │
│              │                │
│              │                │
└────────────────────────────────┘
```

### Отдельные файлы

Два отдельных файла для каждого глаза:
- `photo_left.jpg` - изображение для левого глаза
- `photo_right.jpg` - изображение для правого глаза

## Создание стереоизображений

### Способы получения стереофото:

1. **VR-камеры**: Insta360 EVO, Vuze XR, Kandao QooCam
2. **Стереопары камер**: Два синхронизированных фотоаппарата
3. **Смартфоны**: Приложения для съёмки стереофото (Cardboard Camera, 3D Camera)
4. **3D-рендеринг**: Blender, Unity, Unreal Engine с настройкой стереокамеры

## Устранение неполадок

### "Не удалось инициализировать OpenXR"

1. Убедитесь, что VR-шлем подключён и работает
2. Запустите SteamVR или Oculus App
3. Проверьте, что OpenXR runtime установлен как системный

### "Нет изображений для отображения"

1. Проверьте путь к файлам
2. Убедитесь, что файлы имеют поддерживаемый формат (jpg, png, bmp, tiff)

### Низкая производительность

1. Уменьшите разрешение исходных изображений
2. Закройте другие VR-приложения
3. Проверьте, что GPU драйверы обновлены

## Архитектура

```
sbs_viewer.py
├── StereoImage          # Класс для хранения стереопары
│   ├── from_sbs()       # Загрузка SBS изображения
│   ├── from_separate()  # Загрузка из двух файлов
│   └── create_textures()# Создание OpenGL текстур
│
└── VRStereoViewer       # Основной класс приложения
    ├── initialize_openxr()   # Инициализация OpenXR
    ├── create_session()      # Создание VR сессии
    ├── create_swapchains()   # Настройка рендер-таргетов
    ├── render_frame()        # Рендеринг кадра
    └── run()                 # Главный цикл
```

## Лицензия

MIT License

## Зависимости

- [pyopenxr](https://github.com/cmbruns/pyopenxr) - Python bindings для OpenXR
- [PyOpenGL](http://pyopengl.sourceforge.net/) - Python bindings для OpenGL
- [Pillow](https://python-pillow.org/) - Обработка изображений
- [NumPy](https://numpy.org/) - Математические операции
- [GLFW](https://www.glfw.org/) - Управление окнами и контекстом OpenGL

